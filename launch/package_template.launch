<launch>

  <!-- Launch Package Template Node -->
  <node pkg="package_template" type="package_template" name="package_template" output="screen">
    <rosparam command="load" file="$(dirname)/../config/default.yaml" />
  </node>

</launch>